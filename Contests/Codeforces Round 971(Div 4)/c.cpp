#include <bits/stdc++.h>
using namespace std;
#define ll long long

void mainCode() {
    ll x, y, k;
    cin >> x >> y >> k;

    ll mini = min(x,y);
    ll min_steps = min
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        mainCode();
    }
    return 0;
}
